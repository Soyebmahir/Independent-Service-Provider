import React from 'react';
import { Carousel } from 'react-bootstrap';

const BannerDetails = ({ service }) => {
    const { name, price, img, description } = service;
    return (
        <div></div>
    );
};

export default BannerDetails;